# Review comment example

> This appears to violate `BORING-WEBHOOK-001`: the same delivery can trigger the
> side effect twice. Can we make the handler idempotent and add the linked duplicate
> delivery scenario as a regression test?

The reviewer can follow the ID to the requirement, edge case, verification scenario,
and evidence without copying a page of guidance into the review.
